name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python=3.10
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov


    # Package-dependencies
  - numpy 
  - matplotlib
  - scipy

    # Pip-only installs
  #- pip:
  #  - codecov
