name: test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python>=3.10
  - pip

  # Testing
  - pytest
  - pytest-cov

  # Package dependencies
  - numpy>=2.0
  - matplotlib
  - scipy
