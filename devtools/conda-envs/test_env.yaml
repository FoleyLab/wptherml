name: test

channels:
  - conda-forge
  - defaults

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Package-dependencies
  - numpy 
  - matplotlib
  - scipy
#  - pytorch
#  - scikit-learn
#  - pandas
#  - tqdm


    # Pip-only installs
#  - pip:
#    - pyqubo
  #  - codecov
