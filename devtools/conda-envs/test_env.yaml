name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov


    # Package-dependencies
  - numpy 
  - matplotlib
  - scipy
  - os
  
    # Pip-only installs
  #- pip:
  #  - codecov
