name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov


    # Package-dependencies
  - numpy 
  - matplotlib
  - scipy

    # dependencies for ML interfaces
  - pandas
  - tensorflow
  - sklearn
  
    # Pip-only installs
  #- pip:
  #  - codecov
